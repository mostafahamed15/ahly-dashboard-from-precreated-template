$(document).ready(function() {
    "use strict";

    function e() {
        $(window).innerWidth() > 767 ? (s.removeClass("sidebar-mobile"), o.removeClass("full-width"), t.removeClass("header-hidden"), l.removeClass("full-width"), $(a).hasClass("bars") && a.removeClass("bars").addClass("is-active")) : (s.addClass("sidebar-mobile"), a.removeClass("is-active").addClass("bars"), o.addClass("full-width"), t.addClass("header-hidden"), l.addClass("full-width"))
    }
    $("#menu").metisMenu();
    var a = $(".hamburger"),
        t = $(".header-toggle .header-content"),
        s = $("#left-sidebar"),
        l = $(".footer"),
        o = $("#content");
    e(), $(window).bind("resize", function() {
        e()
    }), a.on("click", function(e) {
        e.preventDefault(), a.toggleClass("is-active")
    });
    var i = $("#nav"),
        n = $("#menu-btn .hamburger-box"),
        r = $("#menu-btn"),
        c = $("#hidden-btn"),
        d = $(".header-left");
    r.on("click", function(e) {
        e.preventDefault(), s.toggleClass("collapse-sidebar"), o.toggleClass("collapse-width"), d.toggleClass("hidden"), r.toggleClass("bg-brand"), l.toggleClass("collapse-footer"), $(i).hasClass("opacity") ? (i.stop().animate({
            opacity: "0"
        }, 0), i.stop().animate({
            opacity: "1"
        }, 2250), i.removeClass("opacity")) : (i.addClass("opacity"), i.stop().animate({
            opacity: "0"
        }, 0), i.stop().animate({
            opacity: "1"
        }, 1e3))
    }), c.on("click", function(e) {
        e.preventDefault(), s.toggleClass("close-sidebar"), o.toggleClass("full-width"), d.toggleClass("hidden"), n.toggleClass("small"), c.toggleClass("bg-brand"), l.toggleClass("full-width"), $(i).hasClass("opacity") ? (i.stop().animate({
            opacity: "0"
        }, 0), i.stop().animate({
            opacity: "1"
        }, 2250), i.removeClass("opacity")) : (i.addClass("opacity"), i.stop().animate({
            opacity: "0"
        }, 0), i.stop().animate({
            opacity: "1"
        }, 2250))
    });
    var p = $(".notifications-scroll");
    p.slimScroll({
        position: "right",
        height: "243px",
        wheelStep: 7,
        touchScrollStep: 200
    });
    var g = $("#btn-mute");
    g.on("click", function(e) {
        $("#icon-notification").toggleClass("fa-bell-slash"), $(".status-pulse").toggleClass("hide")
    });
    var h = $("#btn-slide, #mobile-slide"),
        v = $("#right-sidebar");
    h.on("click", function(e) {
        return v.toggleClass("open-sidebar"), !1
    });
    var C = $("#sidebar-tabs");
    C.on("click", function(e) {
        e.preventDefault()
    });
    var m = $(".chat-item"),
        u = $(".chat-msg");
    m.on("click", function() {
        return u.toggleClass("open"), !1
    });
    var f = $("#close_chat");
    f.on("click", function() {
        u.removeClass("open")
    });
    var b = $(".chat-scroll");
    b.slimScroll({
        position: "right",
        height: "100%",
        wheelStep: 7,
        touchScrollStep: 200
    });
    var w = $("#header");
    $(window).on("scroll", function() {
        window.scrollY > 55 && !$(w).hasClass("header-fixed") ? v.addClass("scroll-sidebar") : v.removeClass("scroll-sidebar")
    });
    var k = $(".msg-scroll");
    k.slimScroll({
        position: "right",
        height: "100%",
        wheelStep: 7,
        touchScrollStep: 200
    });
    var S = $(".todo-scroll");
    S.slimScroll({
        position: "right",
        height: "100%",
        wheelStep: 7,
        touchScrollStep: 200
    });
    var y = $("#search"),
        x = $("#search-btn");
    x.on("click", function() {
        return y.hasClass("open") ? y.removeClass("open") : y.addClass("open"), !1
    });
    var D = $("#mobile-btn");
    D.on("click", function() {
        t.slideToggle(300)
    });
    var T = $("#mobile-menu");
    $("#page");
    T.on("click", function() {
        s.toggleClass("sidebar-mobile"), o.toggleClass("active-bg")
    }), $("[data-toggle=popover]").popover(), $("[data-toggle=tooltip]").tooltip();
    var W = $(".md-ripple");
    W.on("click", function(e) {
        var a = $(this);
        0 == a.find(".ripple").length && a.append("<span class='ripple'></span>");
        var t = a.find(".ripple");
        if (t.removeClass("animate"), !t.height() && !t.width()) {
            var s = Math.max(a.outerWidth(), a.outerHeight());
            t.css({
                height: s,
                width: s
            })
        }
        var l = e.pageX - a.offset().left - t.width() / 2,
            o = e.pageY - a.offset().top - t.height() / 2;
        t.css({
            top: o + "px",
            left: l + "px"
        }).addClass("animate")
    });
    var M = $("#mobile-slide");
    M.on("click", function(e) {
        t.slideToggle(300), e.preventDefault()
    });
    var Y = $("#button-settings"),
        z = $("#theme-settings");
    Y.on("click", function(e) {
        Y.toggleClass("active"), z.toggleClass("active"), e.preventDefault()
    }), o.on("click", function() {
        v.removeClass("open-sidebar"), y.removeClass("open"), u.removeClass("open"), $(window).innerWidth() < 768 && t.slideToggle(300), $(Y).hasClass("active") && $(z).hasClass("active") && (Y.removeClass("active"), z.removeClass("active"))
    });
    var H = $(".header-content .left, .header-content .nav"),
        X = s.add(H).add(h);
    X.on("click", function() {
        y.removeClass("open")
    })
});